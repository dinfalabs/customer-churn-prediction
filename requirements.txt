# Pinned to versions verified to run this project (Python 3.11+; tested on 3.14).
# Models are serialized with this scikit-learn version — keep it in sync to load them.
pandas==3.0.3
numpy==2.4.6
scikit-learn==1.8.0
matplotlib==3.10.9
seaborn==0.13.2
plotly==6.7.0
streamlit==1.57.0
joblib==1.5.3

# Dev / test
pytest==9.0.3
